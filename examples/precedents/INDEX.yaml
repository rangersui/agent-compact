# Example Precedent Index
# Shows the INDEX.yaml format. In your project, this file lives at
# .agent/precedents/INDEX.yaml and indexes your project's precedents.
#
# Agent reads this first. Matches applies_when/tags to current task.
# Only loads full precedent files for matches.
# Precedent files are case reports. This file is Westlaw.

- id: context-is-ephemeral
  holding: State in context window dies on restart or compaction. Critical state belongs in durable storage.
  applies_when: designing agent memory, evaluating state persistence, or relying on prior conversation
  tags: [memory, persistence, architecture, compaction]
  severity: P1
  status: active
  date: 2026-06-21
